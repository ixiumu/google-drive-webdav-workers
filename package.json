{
  "name": "google-drive-webdav-workers",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "build": "wrangler build",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.3",
    "@types/node": "^25.6.2",
    "typescript": "^6.0.3",
    "vitest": "~4.1.5",
    "wrangler": "^4.90.0"
  }
}
